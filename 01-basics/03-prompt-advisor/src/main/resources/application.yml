spring:
  application:
    name: 03-prompt-advisor
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:fake-key-for-tests}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
          max-tokens: 500
