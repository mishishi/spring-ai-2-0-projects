spring:
  application:
    name: project-5-knowledge-hub
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:fake-key-for-tests}
      base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}
      chat:
        options:
          model: ${OPENAI_MODEL:gpt-4o-mini}
